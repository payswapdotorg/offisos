/**
 * Construction Graph bridge module (CAD-IMPLEMENT-003, LOCK-019).
 * Engine-free by construction: imports only domain contracts, the
 * caddocument layer and node:crypto (LOCK-018 scan covers src/graph).
 */

export { bridgeModelHistory, graphNodeId } from "./bridge.js";
