{
  "name": "@offisos/cad-app-shell",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "CAD-IMPLEMENT-001: shared CAD/BIM application shell (Web + Electron) per Architecture v1.1 LOCK-017/018/019. The renderer/editor core, hosts, App API, CADDocument model and adapter boundary are platform-independent; engine coupling is prohibited and enforced by static test.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --import tsx --test \"test/*.test.ts\"",
    "test:forbidden-imports": "node --import tsx --test \"test/no-forbidden-imports.test.ts\"",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
