# RESEARCH-CAD-004 benchmark — exact tested versions
# Python 3.12 required; install with:
#   python3 -m pip install -r requirements.txt
ifcopenshell==0.8.5
cadquery==2.6.1          # provides cadquery-ocp==7.8.1.1.post1 (OCCT 7.8.1 bindings) for the BRep quantity path
numpy==2.1.3
pytest==9.0.2
