# RESEARCH-CAD-002 benchmark — exact tested versions
# The CAD engine (FreeCAD) and harness conventions are identical to
# RESEARCH-CAD-001; this package has no Python engine dependencies of its
# own beyond the test runner.
pytest==9.0.2

# FreeCAD 1.1.3 is NOT pip-installable; it is provided as a pinned AppImage
# (no sudo required). Both `make test/bench` and CI install it reproducibly:
#   version: 1.1.3
#   url:     https://github.com/FreeCAD/FreeCAD/releases/download/1.1.3/FreeCAD_1.1.3-Linux-x86_64-py311.AppImage
#   sha256:  3a853eb69ee595f779f2255dbf80a765926981d8ff68903cefee4dfb03a8f5ef
#   bytes:   820795896
# Setup (extracts to research/cad-002/.freecad/, gitignored; the runner
# also discovers research/cad-001/.freecad/ so both work items can share
# one extraction):
#   mkdir -p .freecad && cd .freecad
#   curl -sL -o freecad.AppImage <url above>
#   echo "3a853eb69ee595f779f2255dbf80a765926981d8ff68903cefee4dfb03a8f5ef  freecad.AppImage" | sha256sum -c -
#   chmod +x freecad.AppImage && ./freecad.AppImage --appimage-extract && rm freecad.AppImage
# The runner discovers FreeCADCmd via $FREECADCMD, .freecad/squashfs-root/usr/bin/freecadcmd,
# research/cad-001/.freecad/..., or PATH.
